spring:
  application:
    name: conflict-tracker
  datasource:
    url: jdbc:h2:mem:conflicttrackerdb
    driverClassName: org.h2.Driver
    username: ifaldmo
    password: ifaldmo
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
